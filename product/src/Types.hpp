#ifndef TYPES_HPP
#define TYPES_HPP

#include <cstdint>
#include <array>
#include <string>
#include <iostream>
#include <vector>
#include <list>

using u8 = uint8_t;
using u64 = uint64_t;
using u16 = uint16_t;
using bb_vector = std::vector<u64>;

#endif // #ifndef TYPES_HPP